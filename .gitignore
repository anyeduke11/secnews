# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg-info/
*.egg
.venv/
venv/
env/
ENV/

# Logs
backend/logs/
*.log
server.log

# Test
.pytest_cache/
.coverage
htmlcov/

# Export (Phase 7: hotspot.db → dsh-SecNews 数据迁移旁路)
# 由 scripts/export_for_dsh.py 运行时生成
data/export/
# 由 scripts/dump_schema.py 运行时生成 (Phase 7d)
data/schema/
# 由 scripts/export_migrations_for_dsh.py 运行时生成 (Phase 7e)
data/migrations/

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Hidden tool / cache folders (2026-07-10)
# 通配符过滤 IDE/agent 工具产生的 . 开头缓存与配置目录
.*/
.arts/
.claude/
.cursor/
.gemini/
.history/
.pi/
.trae/
.web_builder/

# 例外: 以下隐藏目录必须入库 (取消 .*/ 通配)
!.github/
!.trae/

# Hidden single-file configs (2026-07-10)
.mcp.json
.merkle-snapshot.json

# Personal knowledge base (local-only truth source, never publish to GitHub)
# Decision: 2026-08-30 — knowledge/ + llm-wiki-2.0/ removed from git history via filter-repo.
# These dirs are wiki-first source-of-truth, kept ONLY on local disk.
knowledge/
llm-wiki-2.0/

# dsh-SecNews data archive (contains credentials + wiki + db — NEVER publish)
# Decision: 2026-08-30 — archives/dsh-secs-news-2026-08-27.tar.zst contains
# 3 plaintext API keys + 4149 wiki items + 5.2MB db. Removed via filter-repo.
archives/

# Env
.env
.env.local

# Node
node_modules/
frontend/node_modules/

# Build
backend/dist/
build/
frontend/dist/

# Database (Phase 2)
backend/hotspot.db
backend/hotspot.db-wal
backend/hotspot.db-shm
backend/data/*.db
backend/data/*.db-*
hotspot.db
hotspot.db-wal
hotspot.db-shm

# Backup / runtime cache
backend/hotspot.db.bak-*
backend/hotspot.db.bak-dedup-*
backend/cache_data.json
backend/cache_data.bak.*.json
backend/proxy_config.json
backend/backups/
cache_data.db
agent/
tmp/

# Runtime artifacts (never commit)
scripts/logs/
backend/data/export_cache.*
backend/scripts/*.json
backend/cache_data.db
backend/*.pid
frontend/*.pid

# v0.5 M2-T6 分层存储运行时产物 (2026-08-23)
backups/
backend/hotspot-warm.db*
backend/hotspot-cold.db*
hotspot.db.bak-fts-*

# P3-4 Playwright E2E 运行产物 (2026-08-26)
frontend/test-results/
frontend/playwright-report/
