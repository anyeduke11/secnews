# v1.4 Phase 1b 验证清单

> **关联规范**: `2026-07-15-v1.4-phase-1b-spec.md`

---

## Task 2.1: 书签导入

### 后端
- [ ] `backend/services/bookmark_sync.py` 存在，可 import
- [ ] `parse_chrome_bookmarks()` 正确解析嵌套 folder/children 结构
- [ ] `import_bookmarks()` 去重逻辑正确（URL 指纹 + existing items 检查）
- [ ] 书签 folder 名作为 tag 保留
- [ ] 无效 URL 不阻断导入（跳过并计数）
- [ ] `validate=true` 时通过代理验证 URL，无效标记 "dead_link"

### API
- [ ] `POST /api/knowledge/bookmarks/import` 返回 200
- [ ] 响应含 `imported` / `skipped_duplicates` / `skipped_invalid` / `dead_links` 字段
- [ ] 重复导入相同书签，`skipped_duplicates` 正确增加

### 验证命令
```bash
# 导入测试书签
curl -s -X POST http://localhost:8000/api/knowledge/bookmarks/import \
  -H "Content-Type: application/json" \
  -d '{"bookmarks":{"name":"root","children":[{"type":"url","name":"Test","url":"https://example.com/test"},{"type":"folder","name":"安全","children":[{"type":"url","name":"Zero Trust","url":"https://example.com/zt"}]}]}}'
# 期望: {"imported":2,"skipped_duplicates":0,"skipped_invalid":0,"dead_links":0}

# 重复导入
curl -s -X POST http://localhost:8000/api/knowledge/bookmarks/import \
  -H "Content-Type: application/json" \
  -d '{"bookmarks":{"name":"root","children":[{"type":"url","name":"Test","url":"https://example.com/test"}]}}'
# 期望: {"imported":0,"skipped_duplicates":1,...}
```

---

## Task 2.2: 归档同步（手动导入）

### 后端
- [ ] `backend/services/history_import.py` 存在，可 import
- [ ] `import_from_history(item_ids)` 从 hotspots 表读取正确数据
- [ ] URL 去重正确（与现有 knowledge_items 对比）
- [ ] 写入 .md 文件（source="secnews_archive"）

### API
- [ ] `POST /api/knowledge/import-from-history` 返回 200
- [ ] 响应含 `imported` / `skipped_duplicates` / `errors` 字段
- [ ] 不存在的 item_id 在 errors 中报告，不阻断其他导入

### 前端
- [ ] HistoryPage 每个 item 卡片有"📚 归档到知识库"按钮
- [ ] 点击按钮调用 API，成功后显示提示
- [ ] 已导入的条目按钮禁用或变灰

### 验证命令
```bash
# 假设 hotspots 表有 id=1 的条目
curl -s -X POST http://localhost:8000/api/knowledge/import-from-history \
  -H "Content-Type: application/json" \
  -d '{"item_ids":[1]}'
# 期望: {"imported":1,"skipped_duplicates":0,"errors":[]}
```

---

## Task 2.3: 知识图谱

### 后端
- [ ] `backend/services/graph_builder.py` 存在，可 import
- [ ] `build_graph()` 返回 {nodes, edges} 结构
- [ ] 节点 count = len(source_items)
- [ ] 边权重 = concept 共现次数
- [ ] domain 过滤生效
- [ ] 5 分钟缓存生效（相同请求复用缓存）

### API
- [ ] `GET /api/knowledge/graph` 返回 200
- [ ] 响应含 nodes 和 edges 数组
- [ ] `?domain=security` 过滤生效
- [ ] 空数据时返回 `{"nodes":[],"edges":[]}`（不报错）

### 前端
- [ ] `frontend/package.json` 含 echarts + echarts-for-react 依赖
- [ ] `KnowledgeGraph.tsx` 组件存在
- [ ] 力导向图正确渲染（节点可拖拽）
- [ ] 节点大小与 count 正相关
- [ ] 节点颜色按 domain 区分
- [ ] 点击节点触发回调（console.log 或高亮 items）
- [ ] 空状态显示"暂无概念。请先编译知识库"

### 验证命令
```bash
curl -s http://localhost:8000/api/knowledge/graph | python3 -m json.tool
# 期望: {"nodes":[...],"edges":[...]}
```

---

## Task 2.4: 筛选器

### 后端
- [ ] `list_items()` 支持 topic / type / difficulty / since / until 参数
- [ ] SQL 查询正确拼接 WHERE 子句
- [ ] `list_topics(domain)` 返回去重的 topic 列表

### API
- [ ] `GET /api/knowledge/items?topic=zero-trust` 过滤生效
- [ ] `GET /api/knowledge/items?type=analysis` 过滤生效
- [ ] `GET /api/knowledge/items?since=2026-07-01&until=2026-07-15` 时间范围生效
- [ ] `GET /api/knowledge/topics?domain=security` 返回 topic 列表

### 前端
- [ ] `KnowledgeFilters.tsx` 组件存在
- [ ] 4 个下拉（domain / topic / type / difficulty）
- [ ] topic 下拉根据 domain 联动
- [ ] 时间选择（本周 / 本月 / 全部）
- [ ] 筛选变化触发 items 重新 fetch
- [ ] 放在 KnowledgePage items 列表上方

### 验证命令
```bash
curl -s "http://localhost:8000/api/knowledge/items?domain=security&type=analysis&limit=5"
curl -s "http://localhost:8000/api/knowledge/topics?domain=security"
```

---

## Task 2.5: 编译 API

### 后端
- [ ] `backend/services/compiler.py` 存在，可 import
- [ ] `create_compile_task(item_ids)` 创建 knowledge_task 记录
- [ ] 任务文件写入 `knowledge/learning/tasks/pending/task-{timestamp}.md`
- [ ] 任务文件含 YAML frontmatter（task_type / status / created_at / params）

### API
- [ ] `POST /api/knowledge/compile` 返回 200
- [ ] 响应含 task_id / status / items_to_compile
- [ ] 不传 item_ids 时编译所有 compiled=false 条目
- [ ] 传 item_ids 时只编译指定条目

### 验证命令
```bash
# 编译所有未编译条目
curl -s -X POST http://localhost:8000/api/knowledge/compile \
  -H "Content-Type: application/json" \
  -d '{}'
# 期望: {"task_id":1,"status":"pending","items_to_compile":N}

# 检查任务文件
ls knowledge/learning/tasks/pending/
# 期望: task-*.md 文件存在
```

---

## Task 2.6: 健康度审计升级

### 后端
- [ ] `orphan_items` 真实计算（concepts 为空的 items 数）
- [ ] `stale_concepts` 真实计算（30 天未更新的 concepts 数）
- [ ] `gap_analysis` 按 domain 分组计算覆盖度
- [ ] 不再有硬编码 0

### API
- [ ] `GET /api/knowledge/health` 返回 200
- [ ] 响应含 orphan_items / stale_concepts / gap_analysis 字段
- [ ] gap_analysis 每项含 domain / coverage / suggestion

### 前端
- [ ] `HealthDashboard.tsx` 组件存在
- [ ] 展示总条目 / 已编译 / 孤立条目 / 过期概念
- [ ] gap_analysis 以列表或条形图展示
- [ ] 放在 KnowledgePage 左栏下方

### 验证命令
```bash
curl -s http://localhost:8000/api/knowledge/health | python3 -m json.tool
# 期望: orphan_items / stale_concepts 为真实数字，gap_analysis 为数组
```

---

## Task 2.7: SOUL.md

### 后端
- [ ] `backend/services/soul_service.py` 存在，可 import
- [ ] `get_soul()` 读取 knowledge/SOUL.md
- [ ] 首次访问时自动创建默认 SOUL.md 模板
- [ ] `create_soul_task()` 创建 task_type="generate_soul" 任务
- [ ] 任务文件写入 pending/ 目录

### API
- [ ] `GET /api/knowledge/soul` 返回 200
- [ ] 响应含 content 和 exists 字段
- [ ] `POST /api/knowledge/soul/regenerate` 返回 200，含 task_id

### 前端
- [ ] `SoulViewer.tsx` 组件存在
- [ ] 展示 SOUL.md 内容（Markdown 文本，不需复杂渲染）
- [ ] "重新生成"按钮调用 API
- [ ] 放在 KnowledgePage 左栏最下方

### 验证命令
```bash
curl -s http://localhost:8000/api/knowledge/soul | python3 -m json.tool
# 期望: {"content":"# SOUL.md...","exists":true}

curl -s -X POST http://localhost:8000/api/knowledge/soul/regenerate
# 期望: {"task_id":N,"status":"pending"}
```

---

## 整体验证

### 前端 build
- [ ] `cd frontend && npm run build` 成功，无 TS 错误（新增文件）

### 端到端
- [ ] KnowledgePage 三栏布局：左栏显示知识图谱 + 健康度 + SOUL，中栏占位，右栏占位
- [ ] 筛选器在 items 列表上方，筛选生效
- [ ] HistoryPage 有"归档到知识库"按钮
- [ ] 所有新 API 端点返回 200，无 500

### 不破坏 Phase 1a
- [ ] `GET /api/knowledge/items` 仍返回 `{"items":[...],"total":N}`
- [ ] `POST /api/knowledge/sync` 仍正常工作
- [ ] favorites webhook 仍正常工作
- [ ] KnowledgePage 底部 items 列表仍正常显示

### Git
- [ ] 每个 Task 单独 commit
- [ ] commit message 以 `feat(v1.4):` 开头
- [ ] **不执行 git push**（用户要求）
