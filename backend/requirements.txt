# 核心依赖 (runtime)。精确版本见 requirements.lock；
# 开发/测试依赖见 requirements-dev.txt；可选爬虫引擎见 requirements-optional.txt。
fastapi>=0.100
uvicorn[standard]>=0.23
aiohttp>=3.8
pydantic>=2.0
pydantic-settings>=2.0
loguru>=0.7
cachetools>=5.3
APScheduler>=3.10
python-dateutil>=2.8
openpyxl>=3.1
lxml>=5.0
feedparser>=6.0
keyring>=24.0

# P1: knowledge watchdog 文件监听 (knowledge/*.md → SQLite 同步)
watchdog>=6.0

# v1.7 Phase 7: MCP Server (Option A) — hotspot ↔ 外部 AI Agent via MCP
# 锁版本 >= 0.4.0, < 1.0 (fastapi-mcp 1.0 API 仍不稳定)
fastapi-mcp>=0.4.0,<1.0
