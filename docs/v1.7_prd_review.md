# hotspot v1.7 PRD Review — 实测对账 + 关键决策检查

> **作者**: leader skill（MiniMax-M3，2026-07-27）
> **方法**: 实测代码库 + 全文对账 + 给出可粘贴 patch
> **状态**: 待领导拍板（领导看完改判或确认）

---

## 一、PRD vs 实测 偏差表（亲手跑命令验证）

> 调研方法：每个数字都跑过命令。命令贴底部"四、附录：实测命令"。

| 项 | PRD 写 | 实测（2026-07-27） | 偏差 | 严重度 |
|---|---|---|---|---|
| `knowledge/items/*.md` 总数 | 4,127 | **4,147** | +20（轻微过时）| 🟡 P2 |
| **应用 lifecycle 字段的 items 比例** | **25/4127 = 0.6%** | **100%（4,147/4,147）** | **PRD 严重失实** | 🔴 P0 |
| **实际 lifecycle 取值** | "5 阶段 (kl:raw/...) 未落地" | **3 阶段 (`generate`/`signal`/`amplify:tagged`)** | 0% 用 5 阶段值，100% 用 v1.7 旧值 | 🔴 P0 |
| `mcp_tool_registry` 工具数 | 13 | 13 | ✅ | 🟢 |
| `services/*.py` 数量 | 41 | **58** | -17（PRD 写少了）| 🟡 P2 |
| `api/*.py` routers 数量 | 23 | **41** | -18（PRD 写少了）| 🟡 P2 |
| `quality/*.py` gates 数量 | 13 | **22** | -9（PRD 写少了）| 🟡 P2 |
| **实际调度器 jobs 数** | 24 | **30**（已含 kl_trigger_t1-t4 之外的 4 个）| -6（PRD 写少了 6 个）| 🟡 P1 |
| `migrations/*.sql` 文件数 | 24 | 24 | ✅ | 🟢 |
| **SQLite 实际表数** | "37+ 表" | **71**（含 codegarden / favorites / cg_* / knowledge_graph 等）| -34（PRD 严重过时）| 🟡 P1 |
| backend test 文件数 | 67 | **117** | -50（PRD 严重过时）| 🟡 P2 |
| frontend test 文件数 | 未提 | **30** | PRD 缺失 | 🟡 P2 |

### 关键洞察
- **PRD A.1.1 表第 3 行 "应用了 lifecycle 字段的 items 25/4127 = 0.6%"** 是错的。实际情况是 100% 应用了 lifecycle 字段，**但 0% 用过 5 阶段**（`kl:raw` 等）。这不是"99.4% 无 lifecycle"问题，是"100% 用错的 3 阶段"问题。
- v1.7 设计了 5 阶段，**生产数据反而用的 3 阶段**——这才是 PRD 反复强调"5 阶段是基础设施"的真实背景。但 PRD 把 0.6% 写成了 99.4% 未达标的论据，**逻辑方向反了**。
- B.3 架构图里 41 services / 23 routers / 13 gates / 23 schedulers 全部是 v1.4 时代的数字。当前代码库膨胀了 ~40%。

---

## 二、5 阶段 / 5 触发器 / MCP 13 tool / Phase 依赖 — 关键决策点检查

### 2.1 KL 5 阶段 + 5 触发器

| 检查项 | 状态 | 备注 |
|---|---|---|
| 5 阶段定义（raw/refine/link/structure/publish）| ✅ 清晰 | B.6.1 写了状态机、副作用、不变量 |
| 5 触发器职责分工（T1-T5）| ✅ 清晰 | B.6.2 各自写明触发时机、执行主体、副作用 |
| T3 副作用含 chunks_meta | ✅ | B.6.2 T3 已合并到 T3，避免重复 |
| **生产数据生命周期命名迁移** | 🔴 缺失 | PRD 没写从 `generate/signal/amplify:tagged` → `kl:raw/refine/...` 的数据迁移方案 |
| 5 触发器间的死锁 | ✅ 无环 | T1→T2→T3→T4→T5 线性；T5 回 T1 形成环但有用户触发 |
| 5 触发器调度频率 | ✅ 合理 | 30s/10min/30min（与各阶段人工响应时长匹配）|

### 2.2 MCP 13 tool（读独立 + 写副作用）

| 检查项 | 状态 |
|---|---|
| 13 tool 数量 | ✅（已 seed，未变）|
| 6 独立 + 7 副作用分类 | ✅ 有判定原则（B.7.2）|
| 7 个写 tool 失败恢复动作 | ✅ 7/7 都有后手（B.7.3）|
| `add_favorite` 走副作用模式 | ⚠️ 已存在写 `knowledge_items` + `favorites`，但**没标 [A] / [B] 模式**（B.7.1 工具列表需补）|
| `trigger_codegarden_drift` 与 Phase 13 任务重复 | 🟡 B.7.1 写了"新 4 个 tool"，Phase 13.2 又写一遍，建议引用而非重述 |

### 2.3 Phase 依赖图

| Phase | 依赖 | 状态 |
|---|---|---|
| 8 (复利 + 资讯收藏聚合) | 无 | ✅ 可起 |
| 9 (T1/T2 触发器) | 8 (需要 `ai_scores` / `item_entities` 表) | ✅ |
| 10 (抓取层) | 8 (需要 `content_fingerprints`) | ✅ |
| 11 (T3/T4/T5 + 告警) | 9 (T1/T2 跑通才能跑 T3) | ✅ |
| 12 (复利可视化 + 4 模式) | 8, 11 (UI 依赖触发器输出) | ✅ |
| 13 (子系统联动) | 11, 12 (需要 phase 8-12 数据基础) | ✅ |
| 14 (清理 + 文档 + 迁移) | 8-13 全部 | ✅ |
| 15 (Hybrid AI) | 8 (表 + 触发器) | ✅ |
| 16 (Chunks + Attention + 6 模式完整) | 15 + 11 (依赖 T3 写 chunks) | ✅ |

无环 ✅。但 8→9→11→12 强依赖串行，**总预估 46 天已是关键路径**。

### 2.4 资讯收藏聚合视图（Phase 8.9-8.11）

| 检查项 | 状态 | 备注 |
|---|---|---|
| 不新增表 | ✅ 符合 | 逻辑视图（favorites ∪ knowledge_items）|
| favorites 优先去重 | ✅ | 同一 URL 在 favorites 中胜出 |
| 5 类型筛选（ai/security/github/bid/other）| ✅ | B.6.7 完整 schema |
| `since` / `until` 时间筛选 | ✅ | 基于 `favorited_at` / `ingested_at` |
| 与现有 favorites API 关系 | ⚠️ | 已有 `/api/favorites` 列表接口；`/api/knowledge/imported` 与它**不冲突但易混淆**，建议 B.9.6 明确分工 |

### 2.5 6 种认知模式

| 模式 | 实施阶段 | 依赖 | 状态 |
|---|---|---|---|
| 简报 (Brief) | 12 | 已有 dashboard | ✅ |
| 扫描 (Scan) | 12 | 已有 trending | ✅ |
| 深度 (Deep) | 12 | `knowledge_chunks` 表 | ⚠️ chunks 表 B.4.2 SQL 有但 **16 才能跑** |
| 整理 (Outbox) | 16 | reading_states + LLM 摘要 | ⚠️ 16 才有 |
| 复习 (SM-2) | 16 | `sm2_reviews`（已建表）+ LLM | ⚠️ 16 才有 |
| 告警 | 12 | 3 类基础告警规则 | ✅ 11 已铺 |

**结论**: 6 模式"全部实施"成立，但 4 模式 vs 6 模式的实际可用时间差 ~13 天（12 → 16）。

### 2.6 复利公式

| 检查项 | 状态 |
|---|---|
| `50-100 knowledge_links/天`（理想）| ✅ 已加"理想情况下"修饰（L1820）|
| 初期 5-15/天 → 6 个月 30-50/天 | ✅ 现实预期 |
| 复利计算依赖 T2 触发器 + 评分 ≥7 | ✅ 已加 L1820 |

### 2.7 数据模型完整性

| 检查项 | 状态 |
|---|---|
| 6 张新表 SQL | ✅ 4 核心（content_fingerprints/ai_scores/item_entities/knowledge_links）+ 2 扩展（knowledge_chunks/planning_actions/planning_action_log）|
| 索引设计 | ✅ 每张表都有 |
| **5 阶段 lifecycle 字段在 knowledge_items 现有 schema 中** | ⚠️ 现有 schema 用了 `lifecycle TEXT`，但生产数据是 `generate/signal/...`。新写入走 `kl:raw` 没问题，**老数据迁移 SQL 缺失**（B.11.6 迁移策略只字未提）|
| foreign key 引用 hotspots(id) | ✅ 正确 |
| 跨迁移可重入（`IF NOT EXISTS`）| ✅ 全部用 |

### 2.8 验收标准

| 检查项 | 状态 |
|---|---|
| 北极星指标 3 个 | ✅ B.11.1 量化（资讯聚合 P95<300ms 等）|
| Phase 门禁（每 phase 1-3 条）| ✅ B.11.2 列了 |
| Hybrid AI 专项 | ✅ B.11.3 |
| Chunks/Attention/6 模式专项 | ✅ B.11.4 |
| 5 触发器专项测试 | ✅ B.11.5（T1-1 ~ T5-3 共 15 用例）|
| 资讯收藏聚合专项 | ✅ B.11.5 8+7 用例 |
| **反向验证（破坏测试）** | ⚠️ B.11.5 全是 happy path，**没有"破坏 lifecycle 字段后看告警是否响"这类用例**。建议加 |
| 风险与对策 | ✅ B.12 14 条 |

---

## 三、可粘贴 patch（领导扫一眼贴进 PRD）

> 复制每段**整块**粘到对应章节（在文件中 `/^<标题>/` 找位置）。

### Patch 1：A.1.1 第 3 行重写
**位置**: A.1.1 实施数据快照表第 3 行
**原因**: "25/4127 = 0.6%" 数据失实

替换为：
```markdown
| **应用了 `lifecycle` 字段的 items** | **4,147 / 4,147 = 100%** | 全部 items 应有生命周期 | ✅ 字段已建，但值命名与 v1.7 5 阶段不统一 |
| **应用 v1.7 5 阶段 (`kl:raw/refine/link/structure/publish`) 的 items** | **0 / 4,147 = 0%** | v1.7 起新写入 | ❌ **老数据全部用 v1.7 旧 3 阶段值**（见下）|
| **现存 lifecycle 实际取值分布** | `generate`: 74 / `signal`: 4,070 / `amplify:tagged`: 3 | v1.7 目标 5 阶段 | ❌ 与 v1.7 设计 100% 不一致，需 B.11.6 增加迁移 SQL |
```

### Patch 2：B.3 架构图数字
**位置**: B.3 架构图（"关键简化" 之前）
**原因**: services / routers / schedulers / quality gates 全部过时

替换为：
```markdown
│          + 58 services                knowledge/              │
│          + 41 routers                 ├─ items/*.md          │
│          + 13 MCP tools               ├─ concepts/*.md       │
│          + 30 schedulers              ├─ summaries/          │
│          + 22 quality gates           └─ learning/           │
│  数据层  SQLite WAL (71+ 表)         知识文件系统             │
```

### Patch 3：B.11.6 迁移策略 — 补 5 阶段 lifecycle 数据迁移
**位置**: B.11.6 数据迁移（migration 043-045）之后
**原因**: 现有 4,147 items 全部是 v1.7 旧 3 阶段值，PRD 完全没写迁移方案

新增小节：
```markdown
### 5 阶段 lifecycle 数据迁移（migration 046_lifecycle_v2.sql）

**问题**: 生产 `knowledge_items.lifecycle` 字段 100% 有值，但全部是 v1.7 旧 3 阶段（`generate` / `signal` / `amplify:tagged`），与 v1.7 设计 5 阶段（`kl:raw/refine/link/structure/publish`）不一致。

**映射规则**:
- `signal`（未精炼）→ `kl:raw`
- `amplify:tagged`（已打 tag 待关联）→ `kl:refine`
- `generate`（已生成摘要）→ `kl:structure`（v1.7 升级到 kl:structure 等同 generate 的语义，并加 T3 触发 chunks）

**不映射**: 无 lifecycle 值的（如有）→ 保持 `NULL` 留给 Phase 9 T1 触发器处理。

**SQL 草案**:
```sql
-- migration 046_lifecycle_v2.sql
UPDATE knowledge_items
SET lifecycle = CASE lifecycle
    WHEN 'signal'         THEN 'kl:raw'
    WHEN 'amplify:tagged' THEN 'kl:refine'
    WHEN 'generate'       THEN 'kl:structure'
    ELSE lifecycle
END,
updated_at = datetime('now')
WHERE lifecycle IN ('signal', 'amplify:tagged', 'generate');

-- 验证：应输出 0
SELECT COUNT(*) FROM knowledge_items
WHERE lifecycle IN ('signal', 'amplify:tagged', 'generate');
```

**回滚**: 不删原值，迁移 SQL 写在 `046_*_down.sql` 里反向映射。

**验收**: `SELECT lifecycle, COUNT(*) FROM knowledge_items GROUP BY lifecycle` 应只看到 3 个 kl:* 值（外加可能的 NULL 旧值）和 0 个旧 3 阶段值。
```

### Patch 4：B.9.6 加 `/api/knowledge/imported` 与 `/api/favorites` 分工说明
**位置**: B.9.6 关键 endpoint 列表下加 1 段
**原因**: 两个端点都能看收藏，但用途不同，需明确

新增：
```markdown
**`/api/knowledge/imported` vs `/api/favorites` 分工**:
- `/api/favorites`：**只看 favorites 表**（最严格"已收藏"语义），导出 xlsx 用
- `/api/knowledge/imported`：**聚合视图**（favorites ∪ knowledge_items），含时间/类型/名称筛选，用于"看看我导入了什么"的全景视图
- 前端 KnowledgeImport 页面"资讯收藏"卡片 → 调 `/api/knowledge/imported`
- 前端 HotspotCard ⭐ 按钮 → 调 `/api/favorites`
```

### Patch 5：B.11.5 加 1 个反向验证测试
**位置**: B.11.5 测试策略表末尾
**原因**: 现 15 触发器用例全是 happy path

新增 1 行：
```markdown
| 5 触发器 T1 | `test_t1_klraw_alert_red_to_green` | 故意把 1 条 `kl:raw` item 删掉，看告警是否变红；恢复后是否回绿 | 破坏测试 |
```

### Patch 6：B.7.1 13 tool 列表补 `[A] / [B]` 模式标记
**位置**: B.7.1 工具清单表
**原因**: PRD 强调"读独立 / 写副作用"分类，但工具列表本身没标模式

建议在每行加模式列：
```markdown
| 模式 | tool | 失败后手 |
|---|---|---|
| [A] | `search_hotspots` | 无（只读）|
| [A] | `search_knowledge` | 无 |
| [B] | `add_favorite` | favorites unique 不重复；knowledge 提升走 sag_service.try_promote 重试 |
| [B] | `add_annotation` | annotation_service 写表重试 |
| [A] | `update_knowledge_item` | knowledge_repo UPSERT 重试 |
| [A] | `score_item` | rule 评分不依赖 LLM；agent 评分失败回 rule |
| [B] | `enrich_concept` | concept_linker 异步任务入 `dead_letter` |
| [B] | `link_items` | knowledge_links UNIQUE 去重；失败入 dead_letter |
| [A] | `get_knowledge_stats` | 无 |
| [A] | `get_planning_actions` | 无 |
| [A] | `trigger_codegarden_drift` | 已有重试（codegarden_orchestration）|
| [B] | `create_digest` | digest_service 失败入重试队列 |
| [A] | `search_concepts` | 无 |
```

---

## 四、附录：实测命令（领导可复跑）

```bash
# 1. lifecycle 实际分布
.venv/bin/python3 -c "
import sqlite3
conn = sqlite3.connect('backend/hotspot.db')
cur = conn.cursor()
cur.execute('SELECT COUNT(*) FROM knowledge_items')
print('total:', cur.fetchone()[0])
cur.execute('SELECT lifecycle, COUNT(*) FROM knowledge_items GROUP BY lifecycle')
for r in cur.fetchall(): print(' ', r)
"

# 2. services / routers / migrations / tests / quality gates
ls backend/services/*.py | wc -l       # 58
ls backend/api/*.py | wc -l            # 41
ls backend/repository/migrations/*.sql | wc -l   # 24
ls backend/quality/*.py | wc -l        # 22
ls backend/tests/test_*.py | wc -l     # 117
find frontend/src -name "*.test.ts" -o -name "*.test.tsx" | wc -l   # 30

# 3. 实际调度器 jobs
.venv/bin/python3 -c "
import re
with open('backend/scheduler/scheduler.py') as f:
    ids = re.findall(r'id=\"([^\"]+)\"', f.read())
print(f'jobs: {len(ids)}')
for i in ids: print('  -', i)
"

# 4. SQLite 实际表数
.venv/bin/python3 -c "
import sqlite3
c = sqlite3.connect('backend/hotspot.db').cursor()
c.execute(\"SELECT COUNT(*) FROM sqlite_master WHERE type='table'\")
print('tables:', c.fetchone()[0])
"
```

---

## 五、报告结论（领导只看 3 行）

1. **PRD 大方向正确**（5 阶段 + 5 触发器 + 13 tool + 6 模式 + 资讯收藏聚合视图 全部设计自洽），但 **A.1.1 第 3 行 "25/4127 = 0.6%" 数据严重失实**——真实情况是 100% 有 lifecycle 但 0% 用 5 阶段。**P0 必修复**。
2. **B.11.6 迁移策略完全缺 lifecycle 5 阶段数据迁移方案**——4,147 items 等着 v1.7 上线后跑迁移 SQL。Patch 3 已给出可直接用的 SQL。
3. **B.3 架构图 4 个数字（41/23/13/23）全部过时**（实际 58/41/22/30），B.7.1 13 tool 列表缺 `[A]/[B]` 模式标记——Patch 2 + Patch 6 一并修。
