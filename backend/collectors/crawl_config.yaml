# hotspot/backend/collectors/crawl_config.yaml
# v1.7 新增: Crawl4ai 高阶抓取配置
# 代理从 proxy_config.json 统一注入，不在此硬编码

crawl4ai:
  enabled: true  # gateway 方案 §3.1: 生产默认开启 (测试环境经 conftest 指向 disabled yaml 隔离)
  browser: chromium  # chromium | firefox | webkit
  headless: true
  timeout_seconds: 30
  concurrent_requests: 3
  retry_attempts: 2

  proxy_source: "proxy_config.json"
  proxy_failover: true

  anti_bot:
    user_agent_rotation: true
    random_delay_seconds: [1, 3]
    stealth_mode: true

  cache:
    enabled: true
    backend: sqlite
    ttl_seconds: 604800  # 7 天