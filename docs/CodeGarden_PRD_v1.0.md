# CodeGarden（代码花园）v1.0 产品需求文档

> **版本**: v1.0  
> **日期**: 2026-07-19  
> **定位**: 个人氛围编程产物 + AI 协作全生命周期管理平台  
> **部署**: 纯本地单机  
> **开源协议**: MIT License

---

## 目录

1. [产品概述](#1-产品概述)
2. [问题定义与痛点分析](#2-问题定义与痛点分析)
3. [目标用户](#3-目标用户)
4. [功能架构总览](#4-功能架构总览)
5. [核心模块详细设计](#5-核心模块详细设计)
   - 5.1 项目看板模块
   - 5.2 服务网格模块
   - 5.3 资源中枢模块
   - 5.4 联动引擎模块
   - 5.5 生命周期管理模块
   - 5.6 AI 协作管理层
     - 5.6.1 开发 Skill（技能管理）
     - 5.6.2 开发 Memory（记忆管理）
     - 5.6.3 开发 Constraint（AI 行为约束）
     - 5.6.4 Spec 规范（功能规格管理）
     - 5.6.5 SDD 规范（软件设计文档管理）
     - 5.6.6 专业提示词推荐（Prompt 资产库）
6. [数据模型设计](#6-数据模型设计)
7. [交互设计](#7-交互设计)
8. [技术架构](#8-技术架构)
9. [非功能性需求](#9-非功能性需求)
10. [里程碑规划](#10-里程碑规划)
11. [成功指标](#11-成功指标)
12. [附录](#12-附录)

---

## 1. 产品概述

### 1.1 产品名称
**CodeGarden（代码花园）** — 个人氛围编程产物全生命周期管理平台

### 1.2 产品定位
面向个人开发者和独立黑客的一站式氛围编程产物管理系统，解决"开发快、管理乱、维护难"的核心痛点，让氛围编程从"野蛮生长"走向"有序生态"。

**核心定位**：你电脑里所有代码项目的"本地驾驶舱"——从混乱到秩序，只需一个界面。

### 1.3 核心价值主张

> **"从灵感火花到稳定运行，每一个代码产物都有迹可循、有章可依"**

### 1.4 关键决策

| 决策项 | 结论 | 说明 |
|--------|------|------|
| 远程服务器管理 | ❌ 不支持 | 纯本地单机运行 |
| 内置 CI/CD | ❌ 不内置 | 专注管理而非构建流水线，不替代 GitHub Actions |
| 开源协议 | ✅ MIT License | 最宽松，适合个人工具生态 |
| 多用户/团队协作 | ❌ 不支持 | 单用户无登录 |

---

## 2. 问题定义与痛点分析

| 痛点层级 | 具体表现 | 影响 |
|---------|---------|------|
| **开发层** | 代码仓库爆炸式增长，命名混乱，依赖关系不清 | 找代码比写代码更耗时 |
| **部署层** | 端口冲突、服务冲突、环境变量散落各处 | 每次启动都是一次冒险 |
| **运维层** | 不知道哪些服务在运行、哪些已废弃 | 资源浪费，安全隐患 |
| **协作层** | 二次开发他人项目时理解成本高 | 复用率低，重复造轮子 |
| **生命周期层** | 没有版本演进记录，没有淘汰机制 | 技术债累积，系统臃肿 |
| **AI 协作层** | AI 频繁"失忆"，重复解释项目背景；提示词靠运气；AI 乱改核心文件 | 人机协作效率低，质量不可控 |

---

## 3. 目标用户

| 用户类型 | 特征 | 核心需求 |
|---------|------|---------|
| **氛围编程者** | 每天产生大量原型/实验项目 | 快速归档、自动分类、一键启动 |
| **独立开发者** | 维护多个微服务/工具 | 服务拓扑可视化、依赖管理 |
| **技术博主/教育者** | 需要展示和复用代码 | 项目卡片化展示、快速部署演示 |
| **全栈创业者** | 个人维护完整产品矩阵 | 全局资源监控、成本优化 |

---

## 4. 功能架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                         CodeGarden 平台架构                          │
├─────────────┬─────────────┬─────────────┬─────────────────────────┤
│   项目看板   │   服务网格   │   资源中枢   │       联动引擎         │
│  (Project)  │  (Service)  │  (Resource) │    (Orchestrate)        │
├─────────────┼─────────────┼─────────────┼─────────────────────────┤
│ • 项目卡片   │ • 服务注册   │ • 端口池    │ • 依赖图谱              │
│ • 生命周期   │ • 健康检查   │ • 域名映射   │ • 事件总线              │
│ • 版本追踪   │ • 日志聚合   │ • 环境变量   │ • 自动化工作流           │
│ • 标签体系   │ • 配置管理   │ • 密钥托管   │ • 触发器               │
│ • 知识库    │ • 扩缩容    │ • 存储卷    │                         │
└─────────────┴─────────────┴─────────────┴─────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    AI 协作管理层（AI Collaboration Layer）            │
├──────────┬──────────┬──────────┬──────────┬───────────────────────┤
│  Skill   │  Memory  │ Constraint│  Spec   │  Prompt Lib + SDD     │
│  技能定义 │  记忆上下文│  行为约束  │  规格规范 │  提示词资产 + 设计文档  │
├──────────┼──────────┼──────────┼──────────┼───────────────────────┤
│ 角色能力  │ 项目记忆  │ 文件边界   │ 功能契约  │ 场景化 Prompt        │
│ 知识领域  │ 会话上下文│ 代码质量   │ 验收标准  │ 变量自动注入          │
│ 输出格式  │ 长期偏好  │ 依赖管控   │ 接口定义  │ 效果追踪优化          │
│ 协作模式  │ 决策历史  │ 安全红线   │ 数据模型  │ 架构文档守护          │
└──────────┴──────────┴──────────┴──────────┴───────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                        统一数据层 (Data Layer)                       │
│  项目元数据 │ 服务状态 │ 资源占用 │ AI 记忆 │ Spec/SDD │ Prompt │ 事件日志 │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 5. 核心模块详细设计

### 5.1 项目看板模块（Project Board）

#### 5.1.1 项目卡片系统

每个项目以卡片形式呈现，包含：

```
┌────────────────────────────────────┐
│ 🔴 运行中  [项目名]        [⋮]     │
│ 描述：AI对话助手的Web界面            │
│                                    │
│ 标签: #AI #React #实验性             │
│ 生命周期: 活跃开发 → 第3周            │
│                                    │
│ [代码] [文档] [服务] [依赖]          │
│ 最后更新: 2小时前  作者: @me        │
│                                    │
│ 健康度: ████████░░ 80%              │
└────────────────────────────────────┘
```

**项目生命周期状态机：**

```
构思(ideation) → 原型(prototype) → 开发(development) → 
测试(testing) → 运行(running) → 维护(maintenance) → 
归档(archived) / 废弃(deprecated)
```

**自动状态流转规则：**
- 7天无提交 + 无运行记录 → 自动标记"待评估"
- 30天无活动 → 推送"归档建议"
- 依赖存在高危漏洞 → 标记"需修复"

#### 5.1.2 智能分类与标签

- **自动标签**：基于 `package.json`、`requirements.txt`、`Dockerfile` 等自动识别技术栈
- **项目类型**：Web应用 / API服务 / CLI工具 / 爬虫 / 实验脚本 / 库/包
- **重要性评级**：用户手动标记 ⭐ ～ ⭐⭐⭐⭐⭐

#### 5.1.3 项目知识库

每个项目关联：
- `README.md` 自动渲染
- 架构决策记录（ADR）
- 踩坑记录 / 解决方案
- 相关项目链接（双向关联）

---

### 5.2 服务网格模块（Service Mesh）— 本机版

#### 5.2.1 服务注册与发现

自动扫描并注册本机服务：

| 属性 | 说明 |
|------|------|
| 服务名 | 唯一标识，支持命名空间（如 `ai-assistant.web`）|
| 服务类型 | HTTP服务 / WebSocket / gRPC / 静态站点 / 数据库 |
| 运行方式 | Docker容器 / PM2进程 / 系统服务 |
| 健康检查 | HTTP探针 / TCP探针 / 自定义脚本 |
| 资源限制 | CPU / 内存 / 磁盘配额 |

#### 5.2.2 服务拓扑图

可视化展示本机服务间调用关系：

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│  Nginx   │────→│  Web App │────→│  API Svc │
│  (网关)   │     │  (3000)  │     │  (8080)  │
└──────────┘     └────┬─────┘     └────┬─────┘
                      │                │
                      ↓                ↓
                 ┌──────────┐    ┌──────────┐
                 │  Redis   │    │ Postgres │
                 │  (6379)  │    │  (5432)  │
                 └──────────┘    └──────────┘
```

**交互功能：**
- 点击节点查看详情
- 拖拽调整布局
- 实时流量动画（可选）
- 异常服务红色高亮

#### 5.2.3 统一日志与监控

- 自动收集各服务日志，支持关键词搜索
- 服务响应时间、错误率图表
- 资源使用率趋势图

---

### 5.3 资源中枢模块（Resource Hub）— 本机版

#### 5.3.1 端口管理

```
端口池视图：
┌──────┬─────────────┬──────────┬────────┐
│ 端口  │ 占用服务     │ 状态     │ 操作   │
├──────┼─────────────┼──────────┼────────┤
│ 3000 │ web-app     │ 🟢 运行中 │ 释放   │
│ 3001 │ ─           │ ⚪ 空闲   │ 预留   │
│ 8080 │ api-service │ 🟢 运行中 │ 释放   │
│ 5432 │ postgres    │ 🟢 运行中 │ 释放   │
└──────┴─────────────┴──────────┴────────┘
```

**智能端口分配：**
- 自动检测端口冲突
- 启动服务时自动分配可用端口
- 支持端口段预留（如 3000-3010 给前端项目）

#### 5.3.2 域名与路由管理

- 本地开发域名映射（如 `ai-assistant.local` → `localhost:3000`）
- 反向代理配置（Nginx/Caddy 规则自动生成）
- 通过修改本机 hosts 或本地 DNS 实现

#### 5.3.3 环境变量与密钥

- 项目级环境变量模板
- 敏感信息加密存储（AES-256）
- 环境变量差异对比（开发/测试/生产）

#### 5.3.4 存储卷管理

- Docker Volume 映射关系
- 数据备份策略
- 存储空间使用监控

---

### 5.4 联动引擎模块（Orchestration Engine）— 本地事件驱动

#### 5.4.1 依赖图谱

自动解析项目间依赖：
- 代码依赖：A项目引用了B项目的模块
- 服务依赖：A服务调用B服务的API
- 数据依赖：A项目读写B项目的数据库

**依赖影响分析：**
> 修改 `utils-lib` 将影响以下 5 个项目：[list]

#### 5.4.2 事件总线

统一事件系统，支持：

| 事件类型 | 触发条件 | 可执行动作 |
|---------|---------|-----------|
| 代码推送 | Git push | 自动重启关联服务 |
| 服务异常 | 健康检查失败 | 发送通知 / 自动重启 |
| 端口冲突 | 启动失败 | 自动分配新端口 |
| 依赖更新 | 上游项目发布新版本 | 推送更新提醒 |

#### 5.4.3 自动化工作流（Playbook）

可视化编排本机任务：

```
工作流：部署新前端项目
├── 步骤1: 克隆代码仓库
├── 步骤2: 安装依赖 (npm install)
├── 步骤3: 运行测试 (npm test)
├── 步骤4: 构建产物 (npm run build)
├── 步骤5: 分配端口（检查3000-3010范围）
├── 步骤6: 启动服务 (pm2 start)
├── 步骤7: 配置Nginx反向代理
├── 步骤8: 注册到服务网格
└── 步骤9: 发送部署成功通知
```

---

### 5.5 生命周期管理模块（Lifecycle Management）

#### 5.5.1 项目健康度评分

综合评分算法：

```
健康度 = w1×代码活跃度 + w2×文档完整度 + w3×依赖新鲜度 
       + w4×测试覆盖率 + w5×安全评分 + w6×资源利用率
```

**评分维度：**

| 维度 | 权重 | 数据来源 |
|------|------|---------|
| 代码活跃度 | 20% | 最近提交频率 |
| 文档完整度 | 15% | README/文档存在性 |
| 依赖新鲜度 | 20% | 依赖包版本落后程度 |
| 安全评分 | 20% | 漏洞扫描结果 |
| 资源效率 | 15% | CPU/内存使用率 |
| 用户反馈 | 10% | 手动标记/使用频率 |

#### 5.5.2 技术债看板

- 过期依赖列表（含升级建议）
- 安全漏洞追踪
- 性能瓶颈标记
- 待重构模块清单

#### 5.5.3 归档与清理策略

- 自动归档规则配置
- 一键清理废弃容器/镜像/卷
- 项目导出（生成可迁移的压缩包）

---

### 5.6 AI 协作管理层（AI Collaboration Layer）

> **设计哲学**：氛围编程 = 人定义问题 + AI 生成方案。平台必须同时管理「人的意图」和「AI 的输出」，让每一次人机协作都有迹可循、可复用、可优化。

---

#### 5.6.1 开发 Skill（技能管理）

##### 功能定义

将 AI 的"角色能力"资产化、版本化管理。每个 Skill 是一个**可复用的 AI 角色模板**，定义了 AI 在特定场景下的专业能力边界。

##### 数据结构

```json
{
  "id": "skill-uuid",
  "name": "React-Performance-Expert",
  "display_name": "React 性能优化专家",
  "version": "1.2.0",
  "domain": ["frontend", "react", "performance"],
  "capabilities": [
    "识别 React 渲染瓶颈",
    "优化 useMemo/useCallback 使用",
    "分析 Bundle 体积",
    "推荐代码分割策略"
  ],
  "constraints": [
    "不引入新的依赖除非必要",
    "保持现有组件接口不变",
    "优先使用 React 内置 API"
  ],
  "output_format": {
    "code_style": "函数组件 + Hooks",
    "comment_language": "zh-CN",
    "include_benchmark": true
  },
  "system_prompt": "你是一位专注于 React 性能优化的资深前端工程师...",
  "few_shot_examples": [
    { "input": "...", "output": "...", "explanation": "..." }
  ],
  "success_metrics": {
    "bundle_size_reduction": ">10%",
    "render_time_improvement": ">20%"
  },
  "created_at": "2026-07-01",
  "updated_at": "2026-07-19",
  "usage_count": 42,
  "avg_rating": 4.8
}
```

##### 核心功能

| 功能 | 说明 |
|------|------|
| **Skill 市场** | 内置常用 Skill 模板（全栈开发、DBA、安全审计、UI 设计等） |
| **自定义 Skill** | 可视化编辑器：能力清单 + 约束条件 + 系统提示词 + 示例 |
| **Skill 组合** | 一个项目可激活多个 Skill（如「React 专家」+「TypeScript 严格模式」） |
| **Skill 版本** | 迭代优化 Skill 定义，保留历史版本可回滚 |
| **效果追踪** | 记录使用该 Skill 后的代码质量评分变化 |

##### 与项目联动

- 项目创建时**推荐 Skill 组合**（基于技术栈自动匹配）
- 代码审查时**调用对应 Skill** 进行专项审计
- 重构任务前**切换 Skill** 改变 AI 行为模式

---

#### 5.6.2 开发 Memory（记忆管理）

##### 三层记忆架构

氛围编程中，AI 的"失忆"是最大痛点。CodeGarden 构建三层记忆体系：

```
┌─────────────────────────────────────────┐
│  ③ 长期记忆（Long-term Memory）          │
│     跨项目、跨会话的个人编码 DNA          │
│     • 技术偏好（缩进、命名、架构风格）     │
│     • 常用工具链偏好                      │
│     • 个人知识库（踩过的坑、最佳实践）      │
├─────────────────────────────────────────┤
│  ② 项目记忆（Project Memory）            │
│     当前项目的上下文全景图                 │
│     • 架构决策记录（ADR）                 │
│     • 模块依赖关系                        │
│     • 已实现的业务规则                    │
│     • 待办事项与技术债                     │
├─────────────────────────────────────────┤
│  ① 会话记忆（Session Memory）            │
│     当前对话的短期上下文                   │
│     • 本轮修改的文件清单                   │
│     • 当前讨论的功能点                     │
│     • 用户的临时指令                      │
└─────────────────────────────────────────┘
```

##### 核心功能

| 层级 | 功能 | 自动/手动 | 存储位置 |
|------|------|-----------|---------|
| **会话记忆** | 自动记录当前对话修改了哪些文件、产生了哪些决策 | 自动 | 内存 + 本地缓存 |
| **项目记忆** | 项目级知识库：ADR、业务规则、API 约定、数据字典 | 半自动 | SQLite |
| **长期记忆** | 用户画像：编码风格、技术栈熟练度、常见错误模式 | 自动学习 | SQLite |

##### 记忆注入机制

每次与 AI 交互前，系统自动组装 Context：

```markdown
【长期记忆】
- 用户偏好：2空格缩进、函数组件优先、中文注释
- 技术栈：React 18 + TypeScript + Tailwind + Zustand

【项目记忆 - ai-assistant】
- 架构：前端 React(Vite) + 后端 Fastify + PostgreSQL
- 已实现的模块：用户认证、对话管理、消息存储
- 待办：接入 WebSocket 实时推送
- 已知问题：消息列表大数据量渲染卡顿（待优化）

【会话记忆】
- 当前任务：优化消息列表渲染性能
- 已修改文件：MessageList.tsx（添加了虚拟滚动）
- 用户最新指令："用 react-window 替换现有实现"
```

##### 记忆编辑与纠正

- **记忆看板**：可视化查看 AI 记住了什么
- **纠正记忆**：用户可标记"AI 记错了，应该是..."
- **遗忘指令**：支持"忘掉刚才关于 XX 的假设"
- **记忆导出**：将项目记忆导出为 `PROJECT_CONTEXT.md`，供其他 AI 工具使用

---

#### 5.6.3 开发 Constraint（AI 行为约束）

##### 功能定义

定义 AI 的**行为红线与资源边界**，防止氛围编程中的典型事故：AI 乱改文件、引入过多依赖、忽略安全规范、超出预算等。

##### 约束分类体系

```yaml
constraints:
  # 1. 文件系统约束
  filesystem:
    - scope: "只能修改 src/ 目录下的文件"
    - protected: ["package.json", "tsconfig.json", ".env"]
    - max_files_per_session: 10
    - require_approval_for: ["delete", "rename"]

  # 2. 代码质量约束
  code_quality:
    - test_coverage_min: 80
    - max_function_lines: 50
    - max_cyclomatic_complexity: 10
    - forbidden_patterns: ["eval(", "innerHTML", "console.log"]
    - require_types: true  # TypeScript 必须写类型

  # 3. 依赖管理约束
  dependencies:
    - allow_new: "ask_first"  # 引入新依赖需确认
    - preferred: ["lodash-es", "date-fns"]  # 优先使用
    - forbidden: ["moment", "jquery"]
    - max_bundle_size_increase: "50kb"

  # 4. 安全约束
  security:
    - no_secrets_in_code: true
    - sql_injection_check: true
    - xss_prevention: true
    - dependency_vulnerability_check: true

  # 5. 资源约束
  resources:
    - max_tokens_per_request: 4000
    - max_api_calls_per_hour: 100
    - cost_limit_per_day: "$5"

  # 6. 流程约束
  workflow:
    - plan_before_code: true  # 必须先输出方案再写代码
    - test_before_merge: true
    - doc_update_required: ["README.md", "CHANGELOG.md"]
```

##### 约束执行机制

| 机制 | 说明 |
|------|------|
| **预检查** | AI 生成代码前，先检查约束条件是否满足 |
| **实时拦截** | 检测到违规操作时，暂停并提示用户确认 |
| **后审计** | 代码提交后，自动运行约束检查并生成报告 |
| **约束模板** | 按项目类型预设（Web 应用 / API / CLI / 库） |

##### 违规处理策略

- **阻断**：直接拒绝执行（如删除核心配置文件）
- **警告**：执行但标记风险（如引入大型依赖）
- **记录**：仅记录日志（如代码风格偏差）

---

#### 5.6.4 Spec 规范（功能规格管理）

##### 功能定义

Spec 是**人机协作的契约**——在写代码之前，先定义"做什么、不做什么、做到什么程度"。解决氛围编程中"AI 理解偏差导致反复修改"的问题。

##### Spec 模板结构

```markdown
# Spec: 消息列表虚拟滚动优化

## 背景
当前消息列表在 1000+ 条消息时渲染卡顿，FPS 低于 30。

## 目标
- 支持 10,000+ 条消息流畅滚动（FPS > 55）
- 保持现有功能完整（选中、复制、跳转）
- 首屏渲染时间 < 100ms

## 非目标（明确不做）
- 不做消息搜索功能
- 不改动消息数据结构
- 不支持无限滚动加载（保持一次性加载）

## 验收标准（AC）
1. [ ] 使用 react-window 实现虚拟滚动
2. [ ] 动态高度消息项正确计算
3. [ ] 滚动到指定消息功能保留
4. [ ] 单元测试覆盖率 > 80%
5. [ ] 在 Chrome/Firefox/Safari 测试通过

## 技术方案（可选，AI 可补充）
- 库选型：react-window vs react-virtualized
- 实现要点：...

## 关联资源
- 相关 Issue: #123
- 设计稿: [Figma 链接]
- 参考实现: [GitHub 链接]
```

##### 核心功能

| 功能 | 说明 |
|------|------|
| **Spec 创建** | 从模板创建，支持 AI 辅助生成（用户描述需求 → AI 输出 Spec 草案） |
| **Spec 评审** | AI 自检：检查 Spec 是否完整、是否有矛盾、技术方案是否可行 |
| **Spec 锁定** | 确认后锁定，AI 按 Spec 执行，防止 scope creep |
| **Spec 追踪** | 实时显示哪些 AC 已完成，哪些 pending |
| **Spec 版本** | 需求变更时生成新版本，保留变更历史 |

##### 与开发流程集成

```
用户描述需求 ──→ AI 生成 Spec 草案 ──→ 用户确认/修改 ──→ Spec 锁定
                                                          ↓
                                               AI 按 Spec 生成代码
                                                          ↓
                                               自动对照 AC 验收
                                                          ↓
                                               Spec 完成归档
```

---

#### 5.6.5 SDD 规范（软件设计文档管理）

##### 功能定义

SDD 是项目的**架构宪法**——定义系统如何组织、模块如何交互、数据如何流动。AI 在修改代码前必须参考 SDD，防止"局部优化破坏全局架构"。

##### SDD 核心章节

```markdown
# SDD: AI-Assistant 项目架构设计

## 1. 系统架构
[架构图：C4 Model Level 3 - 组件图]

## 2. 模块划分
| 模块 | 职责 | 技术栈 | 负责人 |
|------|------|--------|--------|
| Web Frontend | 用户界面 | React 18 + Vite | AI/人 |
| API Gateway | 路由/认证 | Fastify | AI/人 |
| Chat Service | 对话逻辑 | Node.js | AI/人 |
| Message Store | 消息持久化 | PostgreSQL | AI/人 |

## 3. 数据模型
[ER 图 / 核心实体定义]

## 4. API 设计
[OpenAPI/Swagger 规范]

## 5. 状态管理
[全局状态流：Zustand Store 结构]

## 6. 错误处理策略
[统一错误码 / 日志规范 / 降级策略]

## 7. 安全设计
[认证/授权/数据加密/输入校验]

## 8. 性能基线
[响应时间 / 并发数 / 资源上限]

## 9. 变更日志
[架构决策记录 ADR]
```

##### 核心功能

| 功能 | 说明 |
|------|------|
| **AI 辅助生成** | 基于项目代码自动反向生成 SDD（代码 → 文档） |
| **一致性检查** | 检测代码实现是否与 SDD 定义冲突（如模块间出现未声明的依赖） |
| **变更影响分析** | 修改 SDD 时，自动标记受影响的代码模块 |
| **架构守护** | AI 生成代码时，强制参考 SDD 约束（如"不允许绕过 API Gateway 直接访问数据库"） |

##### Spec vs SDD 关系

| 维度 | Spec | SDD |
|------|------|-----|
| **粒度** | 功能级（一个需求） | 系统级（整个项目） |
| **时效** | 随需求创建和销毁 | 长期维护，随架构演进 |
| **受众** | AI 执行代码时参考 | AI 理解上下文时参考 |
| **产出时机** | 需求阶段 | 设计阶段 / 持续维护 |

---

#### 5.6.6 专业提示词推荐（Prompt 资产库）

##### 功能定义

管理**经过验证的高质量提示词模板**，解决氛围编程中"提示词工程靠运气"的问题。将有效的 Prompt 资产化、可复用、可迭代。

##### Prompt 数据结构

```json
{
  "id": "prompt-uuid",
  "name": "生成 CRUD API",
  "category": "backend",
  "tags": ["fastify", "prisma", "rest"],
  "template": "请为 {{entity}} 生成完整的 CRUD API，使用 Fastify + Prisma。\n\n要求：\n1. 包含列表（分页）、详情、创建、更新、删除\n2. 输入校验使用 Zod schema\n3. 错误处理统一返回 { code, message, data }\n4. 包含单元测试（Vitest）\n\n实体定义：\n{{entity_definition}}\n\n已有模块：\n{{existing_modules}}",
  "variables": [
    { "name": "entity", "type": "string", "required": true },
    { "name": "entity_definition", "type": "text", "required": true },
    { "name": "existing_modules", "type": "text", "required": false }
  ],
  "context_injection": ["project_memory", "sdd_api_design"],
  "example_output": "...",
  "success_criteria": "生成可运行的路由文件 + 测试文件",
  "usage_stats": {
    "used_count": 156,
    "success_rate": 92,
    "avg_tokens": 2400
  },
  "version": "2.1.0",
  "author": "user",
  "is_builtin": false
}
```

##### 核心功能

| 功能 | 说明 |
|------|------|
| **场景模板库** | 内置 50+ 高频场景模板（生成组件、写测试、重构、Debug、Code Review 等） |
| **变量注入** | 自动填充项目上下文（如 `{{project_tech_stack}}` 自动替换为 React+TS） |
| **效果追踪** | 记录每次使用的结果质量（用户评分 + AI 输出是否通过测试） |
| **Prompt 优化** | AI 辅助改进提示词（分析失败案例 → 建议模板调整） |
| **个人 Prompt 市场** | 导出/导入 Prompt，形成个人知识资产 |
| **快捷触发** | 在代码编辑器中 `// @prompt:生成单元测试` 快速调用 |

##### 智能推荐机制

根据当前上下文自动推荐 Prompt：
- 用户在 `*.test.ts` 文件中 → 推荐"生成测试用例"Prompt
- 用户选中一段复杂函数 → 推荐"重构简化"Prompt
- 项目技术栈是 React → 过滤掉 Vue 相关 Prompt

---

## 6. 数据模型设计

### 6.1 核心实体关系

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   Project   │◄─────►│   Service   │◄─────►│   Resource  │
│   项目      │  1:N  │   服务      │  1:N  │   资源      │
└──────┬──────┘       └──────┬──────┘       └─────────────┘
       │                     │
       │              ┌──────┴──────┐
       │              │   Instance  │
       └─────────────►│   运行实例   │
              1:N     └─────────────┘

┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│    Skill    │       │   Memory    │       │    Spec     │
│   技能定义   │       │   记忆      │       │   规格规范   │
└─────────────┘       └─────────────┘       └─────────────┘

┌─────────────┐       ┌─────────────┐
│     SDD     │       │    Prompt   │
│  设计文档    │       │   提示词    │
└─────────────┘       └─────────────┘
```

### 6.2 关键数据表结构（简化）

**projects 表：**

```json
{
  "id": "uuid",
  "name": "ai-assistant",
  "display_name": "AI对话助手",
  "description": "基于OpenAI API的Web聊天界面",
  "type": "web_application",
  "lifecycle_stage": "development",
  "health_score": 85,
  "source": {
    "repo_url": "https://github.com/user/ai-assistant",
    "local_path": "/home/user/projects/ai-assistant",
    "branch": "main"
  },
  "tags": ["AI", "React", "实验性"],
  "priority": 3,
  "created_at": "2026-07-01T10:00:00Z",
  "last_activity_at": "2026-07-19T08:30:00Z",
  "archived_at": null
}
```

**services 表：**

```json
{
  "id": "uuid",
  "project_id": "uuid",
  "name": "web-frontend",
  "type": "http",
  "runtime": "docker",
  "status": "running",
  "endpoint": {
    "host": "localhost",
    "port": 3000,
    "domain": "ai-assistant.local"
  },
  "health_check": {
    "type": "http",
    "path": "/health",
    "interval": 30
  },
  "resources": {
    "cpu_limit": "1.0",
    "memory_limit": "512m"
  },
  "dependencies": ["api-service", "redis"],
  "env_vars": { "NODE_ENV": "production" },
  "created_at": "2026-07-01T10:00:00Z"
}
```

**resources 表：**

```json
{
  "id": "uuid",
  "type": "port",
  "value": 3000,
  "status": "allocated",
  "owner_service_id": "uuid",
  "reserved_until": null,
  "created_at": "2026-07-01T10:00:00Z"
}
```

**skills 表：**

```json
{
  "id": "uuid",
  "name": "React-Performance-Expert",
  "display_name": "React 性能优化专家",
  "version": "1.2.0",
  "domain": ["frontend", "react", "performance"],
  "capabilities": ["..."],
  "constraints": ["..."],
  "output_format": {},
  "system_prompt": "...",
  "few_shot_examples": [],
  "success_metrics": {},
  "usage_count": 42,
  "avg_rating": 4.8,
  "created_at": "2026-07-01T10:00:00Z",
  "updated_at": "2026-07-19T08:30:00Z"
}
```

**memories 表：**

```json
{
  "id": "uuid",
  "type": "long_term | project | session",
  "project_id": "uuid | null",
  "session_id": "uuid | null",
  "category": "preference | decision | context | knowledge",
  "content": "用户偏好2空格缩进",
  "confidence": 0.95,
  "source": "user_explicit | ai_inferred",
  "created_at": "2026-07-01T10:00:00Z",
  "last_accessed_at": "2026-07-19T08:30:00Z"
}
```

**specs 表：**

```json
{
  "id": "uuid",
  "project_id": "uuid",
  "title": "消息列表虚拟滚动优化",
  "status": "draft | locked | in_progress | completed | cancelled",
  "background": "...",
  "goals": ["..."],
  "non_goals": ["..."],
  "acceptance_criteria": [
    { "id": "ac-1", "description": "使用 react-window", "status": "pending" }
  ],
  "technical_approach": "...",
  "related_resources": [],
  "created_at": "2026-07-01T10:00:00Z",
  "updated_at": "2026-07-19T08:30:00Z"
}
```

**sdds 表：**

```json
{
  "id": "uuid",
  "project_id": "uuid",
  "version": "1.0.0",
  "sections": {
    "architecture": "...",
    "modules": [],
    "data_model": "...",
    "api_design": "...",
    "state_management": "...",
    "error_handling": "...",
    "security": "...",
    "performance_baseline": "..."
  },
  "adrs": [],
  "created_at": "2026-07-01T10:00:00Z",
  "updated_at": "2026-07-19T08:30:00Z"
}
```

**prompts 表：**

```json
{
  "id": "uuid",
  "name": "生成 CRUD API",
  "category": "backend",
  "tags": ["fastify", "prisma", "rest"],
  "template": "...",
  "variables": [],
  "context_injection": ["project_memory", "sdd_api_design"],
  "example_output": "...",
  "success_criteria": "...",
  "usage_stats": {
    "used_count": 156,
    "success_rate": 92,
    "avg_tokens": 2400
  },
  "version": "2.1.0",
  "author": "user",
  "is_builtin": false,
  "created_at": "2026-07-01T10:00:00Z",
  "updated_at": "2026-07-19T08:30:00Z"
}
```

---

## 7. 交互设计

### 7.1 主界面布局

```
┌──────────────────────────────────────────────────────────────┐
│  🌿 CodeGarden                    [🔍] [🔔] [👤]            │
├──────────┬───────────────────────────────────────────────────┤
│          │                                                   │
│  📊 看板  │    ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐             │
│  🌐 服务  │    │项目1│ │项目2│ │项目3│ │  +  │             │
│  🔗 联动  │    └─────┘ └─────┘ └─────┘ └─────┘             │
│  ⚙️ 资源  │                                                   │
│  📈 分析  │    [筛选: 全部 ▼] [排序: 最近更新 ▼] [🔍]        │
│  📝 知识  │                                                   │
│          │    列表视图 / 卡片视图 / 拓扑视图                    │
│          │                                                   │
│  ─────── │    ┌─────────────────────────────────────┐        │
│  快捷操作  │    │ 最近动态                            │        │
│  [+新建]  │    │ • 2分钟前 api-service 重启成功       │        │
│  [⚡批量] │    │ • 1小时前 新项目 "data-pipeline" 创建 │        │
│  [🧹清理] │    │ • 3小时前 端口 8080 冲突已自动解决    │        │
│          │    └─────────────────────────────────────┘        │
└──────────┴───────────────────────────────────────────────────┘
```

### 7.2 AI 协作面板布局

```
┌──────────────────────────────────────────────────────────────┐
│  🤖 AI 协作中心                                               │
├──────────┬───────────────────────────────────────────────────┤
│          │                                                   │
│  🎭 Skill │    当前项目: ai-assistant                         │
│  🧠 Memory│                                                   │
│  ⛔ 约束  │    ┌─────────────────────────────────────┐        │
│  📋 Spec  │    │ 活跃 Skill                          │        │
│  📐 SDD   │    │ • React 性能优化专家 [v1.2] ⭐       │        │
│  💬 Prompt│    │ • TypeScript 严格模式 [v1.0]        │        │
│          │    └─────────────────────────────────────┘        │
│  ─────── │                                                   │
│  快捷操作  │    ┌─────────────────────────────────────┐        │
│  [+新对话]│    │ 记忆状态                            │        │
│  [🔄同步] │    │ 长期记忆: 24 条                     │        │
│          │    │ 项目记忆: 156 条                    │        │
│          │    │ 会话记忆: 8 条                      │        │
│          │    └─────────────────────────────────────┘        │
│          │                                                   │
│          │    [🚀 开始新任务]  [📋 创建 Spec]  [💬 快速对话]  │
└──────────┴───────────────────────────────────────────────────┘
```

### 7.3 关键交互流程

#### 流程1：新项目接入

```
用户点击 [+新建项目]
    ↓
输入项目信息 / 自动识别本地项目
    ↓
扫描技术栈和依赖
    ↓
自动分配端口（或复用配置）
    ↓
推荐 Skill 组合（基于技术栈）
    ↓
生成服务配置模板
    ↓
用户确认 → 注册到系统
    ↓
项目卡片出现在看板
```

#### 流程2：AI 辅助开发新功能（完整流程）

```
1. 用户在项目看板点击 [新功能]
   ↓
2. 选择 Skill：「React 性能优化专家」+「TypeScript 严格模式」
   ↓
3. 输入需求描述："消息列表卡顿，需要优化"
   ↓
4. AI 读取 Memory：
   - 长期：用户偏好函数组件、中文注释
   - 项目：当前使用 react-window 已尝试过但有问题
   - 会话：当前在 MessageList.tsx 文件
   ↓
5. AI 生成 Spec 草案 → 用户确认/修改 → Spec 锁定
   ↓
6. AI 参考 SDD（架构约束：不能改动数据层）生成代码方案
   ↓
7. Constraint 检查：
   - ✅ 只修改 src/ 目录
   - ✅ 不引入新依赖（使用已有 react-window）
   - ⚠️ 测试覆盖率 75%（低于 80%，提示补充）
   ↓
8. AI 生成代码 + 测试
   ↓
9. 自动对照 Spec AC 验收
   ↓
10. 更新 Memory：记录本次决策（react-window 方案可行）
   ↓
11. Prompt 效果记录：本次使用的"性能优化"Prompt 成功率 +1
```

---

## 8. 技术架构

### 8.1 系统架构图

```
┌─────────────────────────────────────────────┐
│              前端界面（React + Tailwind）      │
│         看板 / 拓扑图 / 资源面板 / 日志         │
│         AI 协作面板 / Skill 编辑器 / Memory 看板 │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│           本地核心服务（Node.js + Fastify）     │
│  项目扫描 │ 服务管理 │ 端口分配 │ 事件联动       │
│  Skill 引擎 │ Memory 管理 │ Constraint 检查 │    │
│  Spec/SDD 管理 │ Prompt 推荐引擎              │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│           本地数据层（SQLite + 文件系统）        │
│      元数据数据库 │ 配置文件 │ 项目源码（引用）    │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│           本机系统接口层                       │
│   Docker API │ PM2 │ Process │ FileSystem │ Git │
└─────────────────────────────────────────────┘
```

### 8.2 技术选型

| 层级 | 技术选型 | 理由 |
|------|---------|------|
| **桌面端** | **Tauri**（Rust + Web前端） | 比 Electron 更轻量（安装包 < 10MB），性能更好，适合本地工具 |
| 前端 | React 18 + Tailwind CSS + Zustand | 响应式、状态管理简洁 |
| 可视化 | React Flow / Cytoscape.js | 服务拓扑、依赖图谱 |
| 后端 | **Node.js + Fastify** | 与前端同栈，系统调用（child_process, fs）方便 |
| 数据库 | **SQLite**（单文件） | 零配置、便携、备份简单，完美匹配单机场景 |
| 缓存/消息 | 内存 Map + 文件事件监听 | 无需 Redis，单机足够 |
| 进程管理 | 调用 PM2 API / Docker SDK / 原生 child_process | 不重复造轮子 |
| 配置存储 | YAML/JSON | 人类可读、版本友好 |

### 8.3 部署方式

- 单文件可执行程序（Tauri 打包）
- 首次启动自动初始化 SQLite 数据库和配置目录
- 数据目录：`~/.codegarden/`（配置 + 数据库）

---

## 9. 非功能性需求

### 9.1 性能要求

- 看板页面加载 < 2秒（100个项目以内）
- 服务状态更新延迟 < 5秒
- 支持同时管理 50+ 个活跃项目
- AI 记忆注入延迟 < 500ms

### 9.2 可靠性

- 系统自身高可用（watchdog 机制）
- 配置自动备份
- 误操作回滚（如端口释放可恢复）
- AI 约束违规时自动拦截

### 9.3 安全性

- 敏感信息（密钥、密码）AES-256 加密存储
- 环境变量访问权限控制
- 服务间网络隔离（Docker 网络）
- AI 约束防止代码注入和敏感信息泄露

### 9.4 扩展性

- 插件系统：支持自定义扫描器、通知渠道
- API 开放：支持第三方工具集成
- Skill/Prompt 市场：社区共享

---

## 10. 里程碑规划

| 阶段 | 版本 | 周期 | 核心交付 | AI 协作功能 |
|------|------|------|---------|------------|
| **MVP** | v0.1 | 2周 | 项目看板 + 端口管理 | Prompt 库基础版（10 个内置模板） |
| **基础版** | v0.5 | 1个月 | + 服务拓扑 + 健康检查 | + Memory 会话层 + Constraint 基础约束 |
| **完整版** | **v1.0** | 2个月 | + 联动引擎 + 生命周期 | **+ Skill 系统 + Memory 三层体系 + Spec 管理 + SDD 管理 + Prompt 资产库** |
| **生态版** | v1.7 | 4个月 | + 插件系统 | + Prompt 市场 + Skill 共享 + AI 协作数据分析 |

---

## 11. 成功指标（KPI）

| 指标 | 目标值 | 衡量方式 |
|------|--------|---------|
| 项目接入时间 | < 5分钟 | 从发现项目到完成注册 |
| 服务启动成功率 | > 95% | 自动解决冲突后成功启动比例 |
| 找项目耗时 | < 30秒 | 通过搜索/筛选定位项目 |
| 技术债发现率 | 100% | 过期依赖/漏洞是否全部识别 |
| AI 重复解释率 | < 10% | 同一项目上下文 AI 是否需要重复说明 |
| Spec 一次通过率 | > 70% | 锁定 Spec 后 AI 代码无需返工比例 |
| Prompt 复用率 | > 60% | 使用库中模板 vs 从零编写 |

---

## 12. 附录

### 12.1 术语表

| 术语 | 定义 |
|------|------|
| 氛围编程 | Vibe Coding，指借助 AI 辅助快速开发代码的方式 |
| 服务网格 | 系统中所有运行服务的注册、发现、通信管理 |
| 联动引擎 | 协调多个项目/服务之间自动化协作的引擎 |
| 生命周期 | 项目从创建到废弃的完整阶段 |
| Skill | AI 角色能力模板，定义专业领域和行为模式 |
| Memory | AI 上下文记忆，分长期/项目/会话三层 |
| Constraint | AI 行为约束，防止违规操作 |
| Spec | 功能规格文档，人机协作契约 |
| SDD | 软件设计文档，项目架构宪法 |

### 12.2 竞品分析与差异化

| 工具 | 定位 | 不足 | CodeGarden 差异 |
|------|------|------|----------------|
| Portainer | Docker 容器管理 | 只关注容器，不关注代码项目 | 代码-服务-资源全链路 + AI 协作 |
| PM2 Plus | Node 进程管理 | 仅限 Node，无拓扑可视化 | 多运行时支持 + 可视化 + AI 记忆 |
| Nginx Proxy Manager | 反向代理 | 仅代理配置 | 与项目生命周期联动 |
| 自建 Excel/Notion | 手动记录 | 无自动化，易过时 | 自动发现 + 智能管理 + AI 协作 |
| Cursor/Claude Code | AI 编码工具 | 无项目全局管理，AI 易失忆 | 全局管理 + 记忆持久化 + 约束管控 |

### 12.3 开源协议说明

本项目采用 **MIT License**，理由：

- 用户群体是个人开发者，MIT 最无摩擦
- 氛围编程者常需要魔改工具适配工作流，MIT 允许自由修改、闭源分发
- 最宽松的协议有利于社区传播和插件生态
- 本项目定位个人效率工具，非 SaaS 服务

---

> **结语**：CodeGarden 的目标不是取代现有的专业工具（如 Docker、PM2、Nginx），也不是替代 Cursor/Claude Code 等 AI 编码工具，而是成为它们的**统一指挥层**和**记忆中枢**。让个人开发者在氛围编程的浪潮中，既能享受快速开发的快感，又能拥有企业级的管理秩序，更能让人机协作从"一次性对话"进化为"持续进化的伙伴关系"。
